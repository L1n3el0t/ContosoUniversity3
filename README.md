# ContosoUniversity3

1. create an xml file of data from the students model
2. create a class to seed the data and avoid duplicate runs
3. Run the twice to make sure there are no duplicates
4. delete a student to test if a row will be automatically added back
